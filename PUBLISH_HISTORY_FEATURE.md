# 发布历史功能说明

## 功能概述

新增的发布历史功能允许用户查看所有账号的发布记录，包括发布状态、内容详情、时间信息等，帮助用户更好地管理和追踪发布任务。

## 功能特性

### 1. 发布历史记录
- **自动记录**：每次发布时自动记录发布历史
- **详细信息**：记录平台、账号、标题、话题、文件列表、发布状态等
- **状态追踪**：实时更新发布状态（发布中、成功、失败）
- **错误记录**：记录发布失败时的错误信息

### 2. 历史记录查看
- **列表展示**：以表格形式展示所有发布记录
- **筛选功能**：支持按平台、状态、账号名称筛选
- **分页显示**：支持分页浏览，提高加载性能
- **排序功能**：支持按创建时间排序

### 3. 详情查看
- **详细信息**：点击详情按钮查看完整的发布信息
- **内容预览**：查看标题、话题、文件列表等
- **定时设置**：显示定时发布的相关设置
- **错误信息**：查看发布失败时的详细错误信息

### 4. 记录管理
- **删除记录**：支持删除已完成的发布记录
- **状态保护**：发布中的记录不允许删除

## 数据库结构

### publish_history 表

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INTEGER | 主键，自增 |
| task_id | TEXT | 任务ID，用于防重复和追踪 |
| platform_type | INTEGER | 平台类型：1小红书 2视频号 3抖音 4快手 |
| platform_name | TEXT | 平台名称 |
| account_name | TEXT | 账号名称 |
| account_file_path | TEXT | 账号文件路径 |
| title | TEXT | 视频标题 |
| tags | TEXT | 话题标签，JSON格式存储 |
| file_list | TEXT | 文件列表，JSON格式存储 |
| status | TEXT | 发布状态：pending发布中 success成功 failed失败 |
| error_message | TEXT | 错误信息（如果发布失败） |
| publish_time | DATETIME | 发布时间 |
| created_time | DATETIME | 创建时间 |
| enable_timer | INTEGER | 是否启用定时发布：0否 1是 |
| videos_per_day | INTEGER | 每天发布视频数量 |
| daily_times | TEXT | 每天发布时间，JSON格式存储 |
| start_days | INTEGER | 开始天数：0明天 1后天 |
| category | INTEGER | 原创标识：0非原创 其他为原创 |

## API接口

### 1. 获取发布历史
- **接口**：`GET /api/publishHistory`
- **参数**：
  - `page`: 页码（默认1）
  - `pageSize`: 每页数量（默认20）
  - `platformType`: 平台类型筛选
  - `status`: 状态筛选
  - `accountName`: 账号名称搜索
- **返回**：分页的发布历史列表

### 2. 获取任务状态
- **接口**：`GET /api/publishStatus/{task_id}`
- **参数**：`task_id` - 任务ID
- **返回**：该任务的所有发布记录

### 3. 删除发布历史
- **接口**：`DELETE /api/deletePublishHistory/{history_id}`
- **参数**：`history_id` - 历史记录ID
- **返回**：删除结果

## 前端页面

### 发布历史页面 (`/publish-history`)

#### 主要功能
1. **筛选搜索**：支持按平台、状态、账号名称筛选
2. **列表展示**：表格形式展示发布记录
3. **详情查看**：弹窗显示详细信息
4. **记录管理**：支持删除记录

#### 页面组件
- 筛选区域：平台选择、状态选择、账号搜索
- 数据表格：展示发布记录列表
- 分页组件：支持分页浏览
- 详情弹窗：显示完整发布信息

## 使用说明

### 1. 查看发布历史
1. 登录系统后，点击左侧导航栏的"发布历史"
2. 页面会显示所有发布记录
3. 可以使用筛选条件缩小查看范围

### 2. 筛选记录
1. 在筛选区域选择平台类型
2. 选择发布状态
3. 输入账号名称进行搜索
4. 点击"搜索"按钮应用筛选条件

### 3. 查看详情
1. 在记录列表中点击"详情"按钮
2. 弹窗会显示该记录的完整信息
3. 包括基本信息、内容信息、定时设置、时间信息等

### 4. 删除记录
1. 在记录列表中点击"删除"按钮
2. 确认删除操作
3. 注意：发布中的记录无法删除

## 技术实现

### 后端实现
- 数据库表创建和初始化
- 发布历史记录保存和更新
- RESTful API接口实现
- 发布状态实时更新

### 前端实现
- Vue 3 + Element Plus 组件
- 响应式数据管理
- 分页和筛选功能
- 移动端适配

## 注意事项

1. **数据完整性**：发布历史记录会在每次发布时自动创建，无需手动操作
2. **状态更新**：发布状态会根据实际发布结果自动更新
3. **错误处理**：发布失败时会记录详细的错误信息
4. **性能优化**：使用分页加载，避免一次性加载大量数据
5. **移动端支持**：页面已适配移动端显示

## 更新日志

- **v1.0.0** (2024-01-XX)
  - 新增发布历史功能
  - 支持发布记录查看和管理
  - 支持筛选和搜索功能
  - 支持详情查看和记录删除
